import AddMessage from '../Nodes/AddMessage';

const AddNodesPanel = () => {
  return (
    <div className="flex flex-wrap gap-2 p-4">
      <AddMessage />
    </div>
  );
};

export default AddNodesPanel;
